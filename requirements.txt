# medical-uq-selective-prediction
# Exact pins verified to install on Python 3.14 (cp314 wheels), Windows x86-64.
# torch/torchvision are CPU builds from PyPI; for CUDA install the matching wheel
# from https://pytorch.org and the code will auto-detect the GPU.
torch==2.12.0
torchvision==0.27.0
albumentations==2.0.8
opencv-python-headless==4.13.0.92
scikit-learn==1.9.0
numpy==2.4.6
pandas==3.0.3
matplotlib==3.10.9
tqdm==4.68.2
gradio==6.17.3
Pillow==12.2.0
PyYAML==6.0.3
